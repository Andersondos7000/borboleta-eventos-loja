# Supabase Deploy Agent - Variáveis de Ambiente

# Token de acesso do Supabase (obrigatório)
# Obtenha em: https://supabase.com/dashboard/account/tokens
# Permissões necessárias: project:read, project:write, db:write
SUPABASE_ACCESS_TOKEN=sbp_your_token_here

# Referência do projeto (obrigatório)
# Exemplo: abcdefghijklmnopqrst
PROJECT_REF=your_project_ref

# Ambiente (opcional, padrão: staging)
# Valores possíveis: staging, production
ENVIRONMENT=staging

# Modo de simulação (opcional, padrão: false)
# Se true, não aplica migrações, apenas verifica
DRY_RUN=false